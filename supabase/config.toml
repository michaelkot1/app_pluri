# Pluri local Supabase config (minimal).
# Edge Function secrets (never commit real values):
#   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=<real service_role from M0-11>
#
# The iOS app only ships the publishable/anon key. See AGENTS.md §2 / SPEC §15.

[functions.delete-account]
# JWT is verified in the function via auth.getUser(); keep gateway verify on.
verify_jwt = true
